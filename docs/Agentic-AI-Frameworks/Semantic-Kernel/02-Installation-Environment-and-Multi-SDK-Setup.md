# 02 - Installation, Environment, and Multi-SDK Setup

## 2.1 SDK Landscape
Semantic Kernel provides SDKs for .NET, Python, and Java.

## 2.2 Environment Setup
Configure model provider credentials and runtime settings with secure environment variables.

## 2.3 Project Initialization
Start with minimal kernel + model service registration, then layer plugins and agents.

## 2.4 Dependency Discipline
Pin package versions and isolate environments for reproducibility.

## 2.5 Real-Time Example
Team runs shared architecture across Python and .NET services using consistent plugin contracts.
