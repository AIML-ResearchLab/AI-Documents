# 02 - Installation, Environment, and CLI Project Setup

## 2.1 Installation Path
Use `uv`-based installation and project scaffolding recommended in CrewAI docs.

## 2.2 Environment Strategy
Manage API keys and model/provider settings via environment variables.

## 2.3 CLI Workflow
Use CLI scaffolds to generate standard project structure and configuration templates.

## 2.4 Project Layout
Typical layout includes `agents.yaml`, `tasks.yaml`, crew definition, flows, and tools.

## 2.5 Real-Time Example
Team spins up a new automation project with consistent config and deployment-ready layout.
